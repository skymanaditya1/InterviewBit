#include<stdio.h>

int main()
{
		int n;
		scanf("%d", &n);
		if(palindrome(n)==0)
			printf("The number is not a palindrome\n");
		else
			printf("The number is a palindrome\n");
		return 0;
}

int palindrome(int n){
	
}